import createReducer from 'redux-create-reducers';
import update from 'react/lib/update';

const defaultState = {};

let reducer = createReducer(defaultState, (handleAction) => {

})();

export default reducer;
